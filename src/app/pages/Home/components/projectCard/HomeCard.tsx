import type { HomePageSection } from "../../staticData/staticHomeData";
import styles from "./HomeCard.module.css";

type HomeCardProps = {
    section: HomePageSection;
};

export default function HomeCard(props: HomeCardProps) {}
